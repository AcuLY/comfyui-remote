# 主分支保护验证证据 — 2026-07-13

这是活动变更 `rebuild-documentation-governance` 的非规范性外部设置证据。提案、规范、设计和 `tasks.md` 继续拥有权威；本文件只记录用户已批准且已实际生效的 GitHub 仓库设置。

## 授权与对象

- 用户在当前任务中明确允许启用此前约定的最小分支保护配置。
- 仓库：`AcuLY/comfyui-remote`。
- 目标分支：`main`，也是仓库默认分支和草稿拉取请求 `#1` 的合并目标。
- 执行身份对仓库具有 `ADMIN` 权限；认证信息和令牌未写入本证据。
- 配置前，有权限的 GitHub API 请求返回 `Branch not protected`。

## 已生效配置

有权限身份通过 GitHub 分支保护 API 为 `main` 写入并重新读取以下状态：

- `main.protected=true`。
- 必需检查上下文为 `文档治理门禁`，并绑定 GitHub 自动化应用 `app_id=15368`。
- `required_status_checks.strict=true`，合并前必须基于最新目标分支重新满足检查。
- `enforce_admins=false`，不对管理员强制执行。
- `required_pull_request_reviews=null`，不额外增加代码审查人数要求。
- `allow_force_pushes=false`、`allow_deletions=false`。
- 未启用线性历史、会话解决、分支锁定、创建阻断、推送主体限制或派生仓库同步例外。

该配置与用户批准的最小范围一致，没有修改仓库秘密、自动化权限、合并策略或其他分支。

## 检查身份与触发边界

- 工作流路径：`.github/workflows/documentation-governance.yml`。
- 工作流名称：`文档治理`；真实作业显示名：`文档治理门禁`。
- `pull_request` 对所有路径运行，不存在 `paths` 或 `paths-ignore` 过滤器。
- 推送事件只对 `main` 运行，因此功能分支的同名推送检查不能替代拉取请求合并路径使用主分支比较基准的检查。
- 草稿拉取请求 `#1` 在提交 `4ef41293bd61ba4d3629727c677ba3e9d23c0797` 上的 `pull_request` 运行 `29249266543` 成功，作业 `86813541950` 无检查注解；该运行发生在保护写入前，只证明真实检查身份和成功行为。

## 任务处置

- 任务 `13.6` 可以关闭：当前唯一受保护合并目标 `main` 已将真实作业 `文档治理门禁` 设为必需检查，并要求最新目标分支。
- 任务 `13.5` 保持未完成：必须在本配置生效后，由包含后续证据修订版的拉取请求提交重新产生成功的干净 CI，才能作为受保护比较基准证据。
- 任务 `11.1`、`11.3`、`13.7` 和 `13.9` 不受本次仓库设置授权影响，继续保持未完成。

## 恢复边界

配置前状态是 `main` 未受保护。若后续发现该最小配置阻断了未预期工作流，只能在新的明确授权下修改或移除精确保护设置；不得为绕过失败检查而临时关闭必需项。本次没有执行恢复操作。
