# Figma 首轮发现与范围

日期：2026-09-05。状态：P0.a–c、P0.e–f 完成，P0.d 等待用户确认。本文是设计工作方案，不把未审核视觉细节升级为产品决策。

## 已验证

| ID | 检查 | 结果 |
| --- | --- | --- |
| P0.a | 代码与文档 | 新版尚无组件代码、主题 Token 文件或 Code Connect 映射；采用已确认的 CA/VD/IA 约束，不提取旧版 Tailwind/Demo 的组件实现作为新版基底。 |
| P0.b | 文件 | 文件 ftkyOPH4vqv5gWb1HLrPU7 可通过专用工具读取和执行只读脚本；三页 ID 为 0:1、1:2、1:3，只有 1:4 说明文本。所有页面无本地组件，本地变量集合、文字及效果样式均为空。 |
| P0.c | 库 | get_libraries 返回 Material 3、Simple Design System 和 Apple 平台库，没有其他可添加库或下一页；PrimeReact 检索为空。Simple Design System 的 button/background 查询有结果，但这不证明它们符合 PrimeReact v10 的样式与属性合同，未导入。 |
| P0.e | 来源冲突 | 新版设计合同高于旧组件；不把其他库组件冒充 PrimeReact。Figma 中没有既有产品组件与新版合同冲突。 |
| P0.f | 缺口 | 缺少新版主题变量、组件和所有页面首稿；只有说明文本，不存在已实现代码可反向同步。下一阶段只为首轮画面补齐必要设计资产。 |

字体查询确认 Noto Sans SC、Geist、IBM Plex Mono 可用。拟以 Noto Sans SC 绘制中文、IBM Plex Mono 绘制任务 ID/时间等定宽信息；这是首稿建议，不锁定未来字体文件部署方式。

账户查询返回 starter / View。当前只验证了读取，尚未尝试写入或确认写入权限；不据此推断必须购买套餐。免费方案的主题模式等能力应在写入阶段按真实结果处理，不能偷偷取消深浅主题，也不自动升级。

## P0.d 待确认的最小范围

| 编号 | 建议 | 不做 |
| --- | --- | --- |
| F1 | 在现有文件中补画与 PrimeReact v10 对应的最小可复用 Figma 组件，代码阶段仍直接使用 PrimeReact。 | 不购买 UI Kit，不改用 Material/Simple Design System，不从零开发第二套 React 基础组件。 |
| F2 | 先覆盖 Button、InputText、Dropdown、Checkbox、TabMenu、Tag、ProgressBar 及任务列表所需的 DataTable 行结构；菜单、Dialog、Sidebar 随首轮实际状态按需加入。只画首轮会用到的尺寸、状态及属性。 | 不预先穷举所有组件和变体；不按颜色另建多套按钮。 |
| F3 | 统一管理表面、文字、边框、焦点、模块强调、语义状态以及间距/圆角/字号；浅色深色同等设计，图像生产青绿、训练品红/粉。具体数值作为可修改首稿，在创建前登记并随视觉审核修订。 | 不复用旧 Token 数值，不散落硬编码，不把概念色自动变成不可更改产品决策。 |
| F4 | 按已定 FIG-03 绘制对等模块外壳与图像生产任务页，包含桌面明暗对照、移动布局、项目分组、状态筛选和任务详情/失败操作。 | 不扩展所有页面；不引入 KPI 仪表盘；不进入页面代码实施。 |

保留现有三页，用页内 Frame 分区组织组件与画面；不为每个组件另建 Page。仅在用户确认后进入基础样式/组件创建，页面完成后仍单独等待用户审核。
