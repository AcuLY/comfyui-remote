# 文档整理说明

## 来源

- 来源：本轮完整对话形成的 `.tmp/product-refactor/production-feature-inventory-2026-08-26.md`，截至 2026-09-05。
- [原始快照](source-inventory.md) 保留原文；分主题文档不再依赖 ignored 路径。
- 旧源码提交：`3a51ac47f46003d4bbb05ff2d292c6a3edafe5f1`；当前项目未投入使用由用户明确说明，不以旧运行观察推翻。

## 整理规则

- 原有编号全部保留，修正 Markdown 表格中代码片段的竖线转义。
- 最新 A17/A18/A19 的组件、技术与迁移决策高于旧功能行里的实现术语。
- 浏览器 API 使用 Cookie、LoRA 导航用“任务”、恢复/修改历史引用最终章节，消除已被裁决的旧说法。
- 原来的“未来某组决定”若已有结论则改为最终 ID 引用；未完整定义的生命周期细节集中放入最终口径文件，不自行补造产品行为。
- Figma 逐页审核是本次新增确认的工作流要求，单独登记 FIG-01～FIG-06。

## 验证记录

- 编号核对：原清单 588 个唯一条目编号，分主题决策及风险附录覆盖 588/588，无遗漏；原始快照未参与覆盖计数。
- 来源核对：原始快照去除说明前缀并统一 LF 后与源清单完全一致。原始文件保持不变。
- 本目录 19 份 Markdown、28 个本地文档链接检查通过。
- OpenSpec 1.5.0 单变更严格校验通过（使用原检出的现有 CLI，未安装依赖）。
- 新整理文档的 `git diff --cached --check` 通过。原始快照保留原文三行 Markdown 硬换行空格，完整 whitespace 检查会报告这三行，不篡改历史原文以隐藏提示。
- 全量 `docs:check` 未通过环境前置检查：本工作树未安装依赖，缺少固定路径的 OpenSpec binary。已尝试原检出检查器的 `--root` 模式，仍要求目标工作树自身依赖；未为纯文档任务安装旧应用依赖，也未宣称全量检查通过。
- 只整理文档，不运行构建、迁移、数据库脚本、部署或应用测试；不修改旧数据和业务文件。
