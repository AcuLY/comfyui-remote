# 已确认设计的同步记录

日期：2026-09-07。范围：本轮用户已逐组确认的设计与命名，未实施业务代码、视频能力或 Figma 页面。

本记录的首批同步对应提交 `723fa13b`；该批次保留 productionPrompt 字段。随后用户确认 FC-12，维护中的设计与本地 Figma 输入已改为 imageProductionPrompt，原始来源和下方首批验证记录保留当时状态。已核对“整理项目当前状态”任务的正式入口与本轮是同一目录，无需维护另一份当前设计。

## 授权与结果

用户确认了生产公共 Project/Template、按类型划分的小节、图片任务/结果/资产、公共文件夹与排序、图片配置附属对象、项目操作范围，以及 `production`/`training` 代码与路由标识；最后确认所有正式 `LoraTraining*` 实体统一简化为 `Training*`，并要求每轮确认后及时更新文档。视频相关示例名称不作为确定命名。

已同步原决策定义、功能矩阵、API、前端结构、迁移口径、OpenSpec 与本地 Figma 设计输入；集中已确认口径及尚待讨论项见 [命名与小节扩展边界](../decisions/09-forward-compatibility.md)。旧实现列、来源快照及旧运行时文档保持其真实身份。

## 验证

| 检查 | 结果 |
| --- | --- |
| 既有编号 | 原 9 份决策文档的 561 个条目全部保留，无遗漏；新增 IPD-16。旧风险附录及原始清单未修改，FC-01～FC-11 是本轮补充编号。 |
| 历史列 | 对照更新前提交 `d47384f9` 核对 06/07/08 功能矩阵，旧功能名称、入口、状态、依赖和代码证据列均无改动。 |
| 名称与范围 | 当前定义、代码目录、页面和 API 使用已确认命名；不定义视频类型或空接口。`productionPrompt` 与运行数据目录保持既有合同；旧命名只保留在明确的映射、历史来源或原数据目录中。 |
| 本地文档链接 | 检查更新文档的相对文件链接，无缺失目标。 |
| Figma 设计输入 | 本地 Markdown/JSON 矩阵保留 64 个唯一编号，行内容一致，设计仍待用户审核；生产图片生成与批量打码分别保留已有视图。未修改线上节点、Token ID、辅助程序或历史状态快照。 |
| OpenSpec | 使用原检出中已安装且与 package.json 固定版本一致的 OpenSpec 1.5.0，单独验证本变更及全部活动变更；严格校验 3/3 通过，无问题。 |
| Git 差异 | 限定文档范围的 diff/whitespace 检查通过；未纳入运行时、数据、秘密或未确认实现。 |

## 完整文档门禁的限制

通过原检出的现有 `tsx` 与检查器执行完整 `docs:check --root`，目标为本设计检出。检查返回 `tool/configuration`：目标检出缺少本地固定路径 `node_modules/@fission-ai/openspec/bin/openspec.js`。本检出尚未安装依赖，未为纯文档同步安装旧应用依赖或改动门禁配置。

因此完整 `docs:check` 未通过；上述局部完整性与独立 OpenSpec 验证分别报告，不能等同于完整门禁成功。后续逐组讨论继续按已确认规则更新文档，页面审核和业务实施状态保持原样。
