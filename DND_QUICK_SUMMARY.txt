================================================================================
DRAG-AND-DROP IMPLEMENTATION - QUICK SUMMARY
================================================================================

PROJECT: comfyui-remote (Next.js 16 + React 19)

================================================================================
1. LIBRARY STATUS
================================================================================

✅ INSTALLED:
   - @dnd-kit/core@6.3.1
   - @dnd-kit/sortable@10.0.0
   - @dnd-kit/utilities@3.2.2

✅ NO INSTALLATION NEEDED - ready to use

================================================================================
2. ARCHITECTURE OVERVIEW
================================================================================

TWO LAYERS:

Layer 1: Design Demos (/src/app/design-demos/)
   - Visual component showcase
   - Shows drag handle icons
   - NO ACTUAL DRAGGING
   - Components: UnitRowShell, SortableRowShell, FolderRow
   - Status: Display-only patterns

Layer 2: Production (/src/app/projects/, /src/app/assets/presets/)
   - Real application with full DnD
   - Components: SortableFolderRow, SortableGroupCard, SortableCompactCard
   - Status: FULLY WORKING with DB persistence

================================================================================
3. WHAT'S USING DRAG HANDLES
================================================================================

DESIGN DEMOS (Visual patterns):
   ✅ UnitRowShell
      - File: /src/app/design-demos/shared/patterns/index.tsx:28
      - Props: dragHandle, dragHandleClassName
      - Used by: category-form-page.tsx, editor-lora-history.tsx
      - Status: Display only

   ✅ SortableRowShell
      - File: /src/app/design-demos/shared/patterns/index.tsx:392
      - Hardcoded GripVertical icon
      - Index + marker display
      - Status: Display only

   ✅ FolderRow
      - File: /src/app/design-demos/shared/patterns/index.tsx:232
      - Props: showDragHandle, dragHandleClassName
      - Status: Display only

PRODUCTION (Working DnD):
   ✅ Project Folders
      - File: /src/app/projects/projects-client.tsx
      - Component: SortableFolderRow
      - Strategy: verticalListSortingStrategy
      - Works: ✅ Fully draggable, persists to DB

   ✅ Section Cards
      - File: /src/app/projects/[projectId]/section-cards.tsx
      - Component: SortableCompactCard
      - Strategy: rectSortingStrategy (grid)
      - Works: ✅ Grid drag with selection

   ✅ Preset Groups
      - File: /src/app/assets/presets/group-list.tsx
      - Component: SortableGroupCard + SortableGroupMemberItem
      - Nested: Groups + Members (2 DnD contexts)
      - Works: ✅ Both levels independently sortable

   ✅ Folders
      - File: /src/app/assets/presets/folder-components.tsx
      - Component: SortableFolderRow (base implementation)
      - Works: ✅ Simple vertical list

================================================================================
4. IMAGE COMPONENTS STATUS
================================================================================

ReviewImageBoard
   - File: /src/app/design-demos/shared/media/review-image-board/index.tsx
   - Status: Selection UI only, no DnD
   - Has: Image selection, action buttons
   - Missing: onClick handlers on buttons

ResultsPanel
   - File: /src/app/design-demos/features/projects/editor/results-panel.tsx
   - Status: Connected to state, no DnD
   - Has: Image filtering, status buttons
   - Methods: markStatus, toggleFeatured, setLightboxImageId

================================================================================
5. KEY FILES TO UNDERSTAND
================================================================================

REFERENCE DOCS (already created):
   📄 /DND_IMPLEMENTATION_ANALYSIS.md - Full breakdown
   📄 /DND_COMPONENT_REFERENCE.md - Component signatures
   📄 /DND_QUICK_SUMMARY.txt - This file

SOURCE FILES:

   Design Demos:
   - /src/app/design-demos/shared/patterns/index.tsx (UnitRowShell, etc.)
   - /src/app/design-demos/shared/patterns/patterns.module.css

   Production:
   - /src/app/projects/projects-client.tsx (Project folders)
   - /src/app/projects/[projectId]/section-cards.tsx (Section cards)
   - /src/app/assets/presets/group-list.tsx (Preset groups)
   - /src/app/assets/presets/sortable-group-card.tsx (Group card + members)
   - /src/app/assets/presets/folder-components.tsx (SortableFolderRow base)

================================================================================
6. MINIMAL IMPLEMENTATION TEMPLATE
================================================================================

// 1. Import
import { DndContext, closestCenter, PointerSensor, KeyboardSensor, useSensor, useSensors } from "@dnd-kit/core";
import { SortableContext, verticalListSortingStrategy, sortableKeyboardCoordinates, useSortable, arrayMove } from "@dnd-kit/sortable";
import { CSS } from "@dnd-kit/utilities";

// 2. Setup
const sensors = useSensors(
  useSensor(PointerSensor, { activationConstraint: { distance: 8 } }),
  useSensor(KeyboardSensor, { coordinateGetter: sortableKeyboardCoordinates }),
);

// 3. Render
<DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={handleDragEnd}>
  <SortableContext items={items.map(i => i.id)} strategy={verticalListSortingStrategy}>
    {items.map(item => <SortableItem key={item.id} item={item} />)}
  </SortableContext>
</DndContext>

// 4. Component
function SortableItem({ item }) {
  const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({ id: item.id });
  return (
    <div ref={setNodeRef} style={{ transform: CSS.Transform.toString(transform), transition }}>
      <button {...attributes} {...listeners}><GripVertical /></button>
      {/* Content */}
    </div>
  );
}

// 5. Handle
function handleDragEnd(event) {
  const { active, over } = event;
  if (!over || active.id === over.id) return;
  const oldIndex = items.findIndex(i => i.id === active.id);
  const newIndex = items.findIndex(i => i.id === over.id);
  setItems(arrayMove(items, oldIndex, newIndex));
}

================================================================================
7. VISUAL FEEDBACK PATTERNS
================================================================================

During Drag:
   - opacity: isDragging ? 0.5 : 1
   - shadow-lg ring-2 ring-sky-500/30
   - zIndex: isDragging ? 50 : undefined

Hover:
   - cursor-grab (on handle button)
   - active:cursor-grabbing (while dragging)
   - text-zinc-600 hover:text-zinc-400

Container:
   - border-white/10
   - bg-white/[0.03]
   - rounded-xl

================================================================================
8. STRATEGIES & COLLISION DETECTION
================================================================================

Strategies:
   - verticalListSortingStrategy: Single column lists
   - rectSortingStrategy: Grid layouts

Collision Detection:
   - closestCenter: Most common, works well for most cases

Sensors:
   - PointerSensor: Mouse/touch drag (distance: 8px activation)
   - KeyboardSensor: Keyboard navigation with arrow keys

================================================================================
9. WHAT'S MISSING IN DESIGN DEMOS
================================================================================

❌ No DndContext setup
❌ No useSortable hooks
❌ No drag handlers
❌ No state management
❌ No animations
❌ No drag feedback

To add to design-demos:
   1. Wrap components in DndContext
   2. Create sortable wrapper components
   3. Add useState for item ordering
   4. Implement onDragEnd handler
   5. Connect to mock API

================================================================================
10. COMMON ERRORS TO AVOID
================================================================================

❌ Forgetting to pass {...attributes} and {...listeners} to drag button
❌ Not setting ref={setNodeRef} on wrapper div
❌ Forgetting CSS.Transform.toString() for transform style
❌ Using wrong strategy (rect vs vertical list)
❌ Not providing unique IDs for each item
❌ Forgetting to close DndContext
❌ Multiple DndContext without unique IDs

================================================================================
11. TESTING CHECKLIST
================================================================================

When implementing DnD:
   ☐ Drag handle appears
   ☐ Cursor changes to grab/grabbing
   ☐ Items reorder when dragged
   ☐ Visual feedback shows during drag
   ☐ Works with mouse and touch
   ☐ Keyboard navigation works (if KeyboardSensor)
   ☐ Data persists to DB
   ☐ No console errors
   ☐ Works in responsive layouts
   ☐ Nested DnD doesn't conflict

================================================================================
12. RELATED FILES IN PROJECT
================================================================================

State Management:
   - useSortable hook: @dnd-kit/sortable
   - arrayMove utility: @dnd-kit/sortable
   - CSS transforms: @dnd-kit/utilities

Database Persistence:
   - /src/lib/actions.ts: reorderProjectFolders, reorderSections, etc.
   - API: /src/app/api/preset-library/... endpoints

UI Components:
   - GripVertical icon: lucide-react
   - Button component: /src/app/design-demos/shared/primitives/button
   - Selection: CheckSquare, Square icons

CSS:
   - Tailwind: cursor-grab, touch-none, shadow-lg, ring-*
   - Module CSS: patterns.module.css

================================================================================
13. NEXT STEPS
================================================================================

If you want to add DnD to design-demos:

   1. Create sortable wrapper components:
      - SortableUnitRow
      - SortableFolder
      - SortableTemplate

   2. Add showcase page:
      - /src/app/design-demos/showcase/pages/dnd-page.tsx

   3. Implement with mock data:
      - Create state hooks
      - Add handlers
      - Show visual feedback

   4. Optional: Connect to real API:
      - Use reorderProjectFolders, etc.
      - Show persistence

For image mutations (ReviewImageBoard):

   1. Add onClick handlers to action buttons
   2. Connect to editor state methods
   3. Show loading states
   4. Add error handling

================================================================================

📝 Generated: 2026-05-20
🔍 Search breadth: Very thorough
📊 Files analyzed: 16+
✅ Documentation: Complete

For full details, see:
- DND_IMPLEMENTATION_ANALYSIS.md (comprehensive breakdown)
- DND_COMPONENT_REFERENCE.md (signatures and examples)

