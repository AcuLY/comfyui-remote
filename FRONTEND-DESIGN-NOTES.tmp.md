# 前端设计问题记录（临时）

更新日期：2026-09-16。

用途：持续积累前端设计中的重复问题、用户反馈和可验证的改进原则。材料充分后，再由用户决定整理为专门的 skill；当前不是正式 skill，也不替代仓库业务规范。

来源：生产／训练任务页独立原型的两批用户批注及标记截图，共 12 条。第一批 9 条对应 F01～F05；第二批 3 条对应 F06～F08。F01～F05 的“批注”编号均指第一批。记录完成不代表原型已经修正；当前工作仅维护本文档。

## 记录方式与确认边界

- **明确要求**：用户直接提出的要求，按原意执行，不因润色而弱化。
- **归纳原则**：从具体问题提炼的通用检查方法，注明适用边界，不能冒充用户逐字确认的规则。
- **业务特例**：保留成立条件；不能脱离业务，把一次选择变成通用组件禁令。
- **待确认**：只有影响后续方案且无法从上下文判断的分歧才向用户确认；不为已明确的偏好反复询问。
- 后续反馈归入已有条目，新增反例或修订边界；仅出现不同根因时新建条目。用户后续明确修订时，留下修订日期与理由。
- 分开记录“设计原则已明确”和“界面修改已完成”，不因记录或技术检查通过而声称获得设计认可。

## F01：用没有任务价值的次级文本填充界面

**来源：批注 1、8。明确要求：禁止无意义的装饰性次级文本。**

本次反例：

- 标题下的“从输入到结果，查看每一次图片生产。”没有提供筛选、执行、结果或操作上的新增信息。
- 详情操作栏中的“历史任务的输入保持不变”与此处的暂停／取消操作无直接关系，只是在陈述内部规则。

归纳原则：文案必须帮助用户定位、识别对象、判断状态、做出选择、完成操作或理解必要后果。不能仅因为标题下、页脚或按钮旁留有空间，就补上一句说明。

处理方法：

- 对每句辅助文案问：删除后，用户会少知道什么，会在哪个具体判断或操作上出错？无法给出具体答案就删除。
- 有价值但出现位置不对的信息，应移到真正涉及该规则的操作或内容附近，而不是常驻所有页面。
- 不要把删除的空话替换为更短的空话；布局应适应实际信息量。

边界：失败原因、等待原因、字段格式、有效输入要求、不可逆操作后果等可以保留。次级文本的字号、颜色或长度不是判断其价值的依据。例如，解释重试会从头执行还是从检查点继续，直接影响决策，不能为追求简洁而删除。

检查：每条可见辅助说明都能对应一个具体用户问题，并出现在用户需要答案的位置。

## F02：把原型制作说明混进产品界面

**来源：批注 2、3、4、9。明确要求：原型按最终生产前端设计，不出现描述原型本身的文字。**

本次反例：

- 侧栏：“本地交互原型”。
- 顶部：“原型 · 示例数据”。
- 列表底部：“时间为示例时间 · 图片使用示意占位”。
- 结果区域：“图片为原型占位，可点击预览。导出内容为示例结果清单。”

归纳原则：被评审的产品界面只承载最终用户所需的信息，不承担向评审者解释制作方法、演示环境、素材来源或实现完成度的职责。

处理方法：

- 在交付回复或独立说明文档中统一说明使用演示数据、未连接后端、哪些操作仅演示；不要将这些内容写进产品页头、页脚、提示条或操作反馈。
- 按真实场景准备连贯的数据与素材，避免为了方便制作，给每个内容区贴上“示例”“占位”“原型”标签。
- 检查范围包含弹窗、空状态、通知、悬浮提示和图片预览，不能只删首屏上的几处文字。
- 评审调节工具若确实需要，应与产品界面隔离，避免成为最终产品设计的一部分。

边界：这是界面表达要求，不是允许对交付状态作虚假陈述。仍需在界面外如实说明演示范围，不声称已执行真实生产、训练、下载模型或部署。最终产品本身确有测试环境提示需求时，按其业务需求另行设计，不能据此默认给所有原型加标记。

检查：把画面直接当作最终产品交给使用者时，不会出现只有制作者或评审者才需要理解的说明。

## F03：同一事实在多个元素中重复表达

**来源：批注 5、7。明确要求：逐个检查信息是否重复，不能仅为美观添加冗余。**

本次反例：

- 面包屑“生产／全部任务”已交代页面位置，又使用“生产任务”大标题和“任务工作台”标签重复定位，占据大量首屏空间。
- 详情标题右侧已有“执行中”，下方“执行概况”又列出“当前状态：执行中”。

归纳原则：审查信息本身，而非仅审查文字是否相同。同义标题、图标标签、状态字段和说明句可能表达同一个事实。增加视觉层级不等于增加信息价值。

处理方法：

- 给每个元素写出职责，例如路径导航、对象识别、状态判断或操作入口。职责与邻近元素重叠时，优先合并或删除。
- 页面定位保留足够清晰的一组表达；不默认叠加面包屑、大标题、副标题、栏目标签。
- 状态保留在用户最容易发现且能关联到对象的位置；其他位置只增加原因、阶段或处理方式等新信息。
- 先减少冗余结构，再调整间距；不能只缩小字号和空白，把重复信息继续保留下来。

边界：不是一律禁止面包屑和标题共存。面包屑用于可操作的上级路径、标题用于具体对象名称，或移动端不再显示面包屑时，两者可能各有职责。列表与独立详情分别显示对象状态也可以成立；需要重点检查的是同一阅读上下文中的无效重复。

检查：每个重复出现的事实都能说明第二处展示的独立用途。评审应看实际桌面与手机画面，不能仅凭 页面结构 中存在元素认定其重复。

## F04：先套组件模板，再把业务塞进去

**来源：批注 6。业务条件与方向已明确，通用原则为归纳。**

本次业务条件：用户说明当前系统同一时刻只会有一个任务实际被 ComfyUI 执行，或进行训练。界面应围绕这个唯一的当前执行任务组织关注点。

本次问题：把“执行中”与所有历史状态平铺为同权筛选 标签页，机械映射状态枚举。用户必须切换筛选才能聚焦当前唯一执行对象，而页面没有直接体现单任务执行的业务结构。

本项目设计方向：将当前执行任务提取为可直接查看的区域，围绕该任务呈现身份、进度、耗时和适用操作。等待队列与历史记录承担各自的浏览职责。“执行中”不再仅作为普通状态筛选入口承载实时任务。

归纳原则：先确定对象数量、并发约束、状态之间的关系、用户查看频率和主要动作，再选择 标签页、列表、独立区域或其他结构。不要把后端枚举逐项映射为同等权重的 UI。

处理方法：

- 先回答：最多有几个同时执行的对象？用户首先关心当前执行、待处理还是历史检索？哪些信息必须无需切换就能看到？
- 只有一个高关注对象时，优先考虑直接呈现；多个并发对象时，再考虑集合浏览与筛选。
- 真实状态仍需完整表达，但“状态完整”不要求每个状态都占一个一级导航位置。
- 演示数据也应遵守业务不变量。此前原型在生产和素材生成中各放了一个执行中任务，不能继续用这种数据支撑新的单任务执行设计。

边界：不能泛化为“禁止执行中 标签页”“所有任务页必须顶部卡片”或“所有系统只能单任务”。提取当前执行对象不等于强制某一种卡片样式，更不等于增加一组统计数字。

实施前待确认：全局唯一执行任务属于另一模块或另一个项目时，当前页面是否仍展示它；若展示，需要与当前列表的筛选范围清晰区分。这影响跨模块与项目范围的交互，本文不擅自确定。

检查：无需切换筛选即可理解当前执行情况；队列与历史浏览没有被打断；所有样例符合并发约束；未凭空增加本业务不存在的任务或状态。

## F05：为了排列整齐，把语义不同的字段放进同一组

**来源：批注 7。明确要求：并列展示前，检查元素是否真的具有并列关系或共同意义。**

本次反例：“执行概况”下并列当前状态、执行尝试、生成模型、输出尺寸。模型和画幅是输入配置，不能因为四个字段恰好能排成两行，就放到执行信息组中；当前状态又与标题标签重复。

归纳原则：视觉分组是在向用户声明这些信息存在关系。先建立语义分组，再选择列数、对齐与留白，不能反过来为网格找字段。

处理方法：

- 用一个明确的用户问题定义每个分组。例如“执行到哪一步”“这次使用了什么输入”“产出了什么”。
- 执行信息按需要容纳阶段、进度、时间、尝试或失败原因；模型、尺寸、采样参数归入输入配置；图片与模型文件归入结果。
- 字段应按实际阅读与比较关系并列，不仅因为它们都是短文本或都有标签和值。
- 删除重复字段后重新判断分组是否仍有必要；只剩一个字段时，不必继续保留整块概况容器。

边界：分组取决于用户正在完成的任务。排查某个模型导致的执行异常时，模型信息可以与异常建立直接联系；但这种关联应有明确用途，不能用“可能有用”解释任意混排。

检查：组内每个字段都能回答该组声明的问题；没有为凑列数加入的字段；组名真实概括内容，没有“概况”“信息”等宽泛名称掩盖混杂关系。

## F06：图标与文字在视觉上没有对齐

**来源：第二批批注 1。明确要求：图标和文本必须保持对齐。**

本次反例：侧栏品牌区的图标偏高，品牌文字偏低，两者没有形成稳定的同行关系。

归纳原则：以实际可见图形和文字的视觉关系判断对齐，不能把容器设置了居中当作对齐完成。矢量图标 内部留白、字体字面与行盒、图标外层容器都可能使数学居中与视觉居中不同。

处理方法：

- 先确定组合的对齐基准：按钮、导航等短标签通常按视觉中心对齐；正文内的图标可能按文字基线对齐。选定后，同类组合保持一致。
- 先检查字体行高、图标尺寸、矢量图标 可见轮廓和外层容器，再调整布局。不要一开始就用任意位移补偿，掩盖结构问题。
- 核对图标与文字组成的整体，而非分别查看两个元素的边界框。必要的光学校正应小而一致，只适用于对应的图标和文字组合。
- 在实际使用的字号、字体与中英文内容下检查，覆盖会改变尺寸或对齐关系的响应式状态。

边界：对齐不等于所有图标与文字都强制使用同一种 样式 基准，也不等于把所有图标设置成相同的可见宽高。要求是组合视觉稳定，且相同用途的组合采用一致规则。

检查：实际画面中没有明显的图标上浮、文字下沉或相邻组合基准跳动；不能仅凭 `align-items: center` 或几何数值宣称通过。

## F07：没有新增用途，却增加重复的操作入口

**来源：第二批批注 2。明确要求：增加组件前，检查同一功能是否已有合适的交互入口。**

本次反例：任务卡片上的“查看详情”按钮，与卡片进入详情的交互意图重合。用户质疑它是否值得单独占据一个按钮和操作区域。

证据边界：这是对交互设计的反馈，不据截图断言整张卡片已经实现点击。后续修改时，应核对当前实际可点击的是标题、卡片还是按钮，避免只删除按钮却留下不可发现或不可访问的入口。

归纳原则：信息冗余与操作冗余分别检查。同一对象、同一目的地、同一结果的重复入口，需要明确的额外价值，不能仅因卡片模板有“操作区”就补上按钮。

处理方法：

- 在新增入口前列出当前完成该动作的路径，判断已有入口是否足够可发现、可理解、易于点击，并支持键盘操作。
- 已有入口满足这些要求时，优先保留一个清晰的主入口；不自动叠加“查看”“详情”“进入”等同义按钮。
- 若卡片作为详情入口，必须处理选中复选框、项目链接等子操作的独立性，不能让选择任务或跳转项目误触发详情。
- 点击区域需要正确的语义、焦点和键盘行为；不能只给普通容器添加鼠标事件，也不能将多个交互控件非法嵌套到同一个按钮中。
- 删除重复入口后，重新收拢操作区域和卡片高度，避免留下空的布局位置。

边界：不是禁止一个功能有多种访问方式。明确按钮若承担必要的可发现性或可访问性，或者整卡包含多个独立动作、不适合整体点击，就可以作为主要入口保留。应选择合适的主交互，而不是为了“少一个按钮”牺牲可用性。

检查：每个重复入口都能说明额外用途；主入口在鼠标、触摸和键盘下可用；子操作不与主入口冲突。

## F08：容器尺寸与信息组织不匹配，造成无效空白

**来源：第二批批注 3。明确要求：谨慎使用空间，避免大量浪费。**

本次反例：任务网格卡片较宽、较高，文本和状态却主要沿左侧单列堆叠，右侧长期闲置，信息组之间又存在较大空档。同一屏可浏览的任务因此减少，增加了滚动和比较成本。

归纳原则：留白应服务于分组、层级、阅读或操作，而不是由固定尺寸、模板比例和机械堆叠偶然产生。检查信息组织与可用空间是否匹配，不能只检查有没有溢出。

处理方法：

- 先检查容器宽高、图片比例、列数和信息布局是否适合实际内容。不要默认把列表内容纵向堆叠后装进大卡片。
- 语义相关且适合比较的信息可以利用横向空间组织；不相关字段不能为了填空而强行并列，仍需遵守 F05。
- 优先调整布局、约束宽度、减少不必要高度和组间空档，再考虑缩小字距或字号；保留可读性和足够的操作命中区。
- 在桌面、窄屏和内容较短／较长的样例下观察空间利用。不能因为某一断点不溢出，就认定它的密度与布局合理。
- 比较调整前后，同一视口中能否更快识别、比较和处理任务，而非只比较卡片是否显得“饱满”。

边界：不要求填满每一块空白，不增加无用文案、指标或按钮来占位置。为阅读节奏、操作隔离和状态变化预留的空间可以成立；真实图片的展示比例也可能需要保留。目标是有效利用空间，不是最大化信息密度。

检查：明显空白能够说明用途；内容没有无理由地挤在一侧；卡片尺寸与内容量相称；提高密度没有破坏语义分组或操作可用性。

## 下次设计时的简短检查

1. 先写清用户要判断和操作什么，以及真实业务约束，再选页面结构与组件。
2. 每个可见元素是否提供独立信息或必要操作？删除后具体损失什么？
3. 是否存在同一位置、状态或数量的重复表达？重复是否有独立用途？
4. 每个信息组是否围绕同一个问题，字段是否真的适合并列？
5. 页面中是否混入对原型、制作过程或实现程度的说明？将这些说明移到交付材料。
6. 图标与文本是否在实际画面中视觉对齐，而不只是容器几何居中？
7. 每个操作入口是否提供独立价值？减少重复入口后，主操作是否仍可发现、可访问，且不与子操作冲突？
8. 容器尺寸、内容组织和信息密度是否匹配？明显空白有何用途，是否增加了无谓滚动？
9. 以实际画面和使用顺序复核，而非以“能运行、无溢出、组件齐全”替代设计判断。

## 后续沉淀为 skill 时

- 以稳定的决策方法为主体，保留简短正反例和可观察的检查方法。
- 项目特有的单任务执行等约束留在项目上下文；skill 教如何识别并使用这些约束，不硬编码本项目布局。
- 尚未确认的跨模块展示行为不写成硬规则。
- 不把本次原型中的技术选型、颜色或布局视为用户认可；也不因记录存在就自动改写其他技能。
- 继续积累反馈后，由用户确定何时整理与启用正式 skill。
