@AGENTS.md

# Claude Agent Entrypoint

AGENTS.md is the source of truth for repository workflow rules. This file exists only for Claude-style clients that auto-open `CLAUDE.md`; read `AGENTS.md` and the linked `agent-rules/**` files before acting.

Do not add divergent workflow instructions here. If a rule changes, update the source `agent-rules/**` file and the rendered `AGENTS.md` summary in the same documentation batch.
