# Script Maintenance Matrix

This is the maintained owner map for repeatable repository scripts. Update it whenever a file under `scripts/**` or `src/scripts/**` is added, removed, renamed, or changes its CLI contract.

## Script Contracts

| script | owner | purpose | inputs | outputs | dry-run or preview behavior | exit code behavior |
| --- | --- | --- | --- | --- | --- | --- |
| `scripts/auto-censor-mosaic.py` | censoring | Python auto-censor runner for single images or batch manifests using YOLO and OpenCV mosaic output. | `--model`, `--classes`, `--mosaic-size`, and either `--input` plus `--output` or `--batch`. | Processed image files and per-item JSON stats on stdout. | `--help` previews CLI shape without loading heavy model dependencies. | exits 0 on successful processing, non-zero on validation, model, image, or JSON failures. |
| `scripts/cleanup-latent-artifacts.mjs` | maintenance-scripts | Removes legacy `latents` directories and `.latent` files below one or more roots. | One or more root paths as positional arguments. | First a JSON deletion plan with exact directories and files, then a JSON summary with removed counts. | dry-run is not supported yet, but the exact plan line is printed before deletion. | exits 0 after cleanup, non-zero on missing arguments, non-directory roots, or filesystem errors. |
| `scripts/db/collapse-preset-group-bindings.ts` | preset-library | Collapses redundant preset-group binding rows in SQLite data while preserving group-level references. | `--database-url`, `--dry-run`, `--write`, `--format`, `--json`, or `--summary`. | Summary or JSON report with updated references, deleted bindings, prompt blocks, and moved LoRA entries. | `--dry-run` previews the plan without writes, and `--write` is required for mutation. | exits 0 when the plan or write succeeds, non-zero on invalid args, unsafe flag combinations, or DB errors. |
| `scripts/db/migrate-zero-redundancy.ts` | data-model | Builds or applies the zero-redundancy migration plan for prompt and LoRA relation rows. | Provider or source DB options, batch size, format, dry-run or write flags, and optional verifier args. | Summary or JSON migration plan and optional verification report. | dry-run and read-only modes preview changes before `--write` applies them. | exits 0 when planned work and verification pass, non-zero on invalid input, migration errors, or failed verification. |
| `scripts/db/verify-zero-redundancy.ts` | data-model | Verifies resolver output against legacy prompt and LoRA snapshots after zero-redundancy migration. | `--read-only`, `--format`, and `--allow-mismatch`. | Summary or JSON report with mismatch, invalid JSON, and invalid reference counts. | read-only verification is the normal preview behavior and does not mutate data. | exits 0 when accepted, otherwise non-zero bit flags identify invalid JSON, invalid references, or resolver mismatches. |
| `scripts/docs/generate-prisma-schema-compatibility.ts` | documentation-system | Regenerates the PostgreSQL and SQLite Prisma schema compatibility report. | Current Prisma schema files from `prisma/**`. | `docs/prisma-schema-compatibility.md`. | no direct dry-run, but generated diff can be reviewed before staging. | exits 0 after writing the report, non-zero on schema read or generation errors. |
| `scripts/docs/generate-repo-inventory.ts` | documentation-system | Regenerates the tracked-file inventory with area, owner, role, target, and action metadata. | `git ls-files` plus current inventory artifacts. | `docs/repo-inventory.md`. | no direct dry-run, but generated diff can be reviewed before staging. | exits 0 after writing the inventory, non-zero on git or file write errors. |
| `scripts/fix-position-presets.py` | preset-library | Legacy helper for preset-position data repair. | Local preset-position source data expected by the script. | Updated preset-position data or console diagnostics depending on invocation. | dry-run is not supported, so inspect the script and run on a copy when unsure. | exits 0 when the repair path completes, non-zero on Python exceptions or missing inputs. |
| `scripts/migrate-preset-variants.sql` | preset-library | SQL maintenance input for preset variant migration. | Database connection chosen by the caller and this SQL file. | Database schema or row changes executed by the SQL client. | preview by reading the SQL and running inside a transaction or disposable database. | exit behavior is owned by the SQL client, with 0 on success and non-zero on SQL errors. |
| `scripts/migrate-sqlite.sql` | maintenance-scripts | SQL maintenance input for SQLite migration setup. | SQLite database selected by the caller and this SQL file. | SQLite schema or row changes executed by the SQL client. | preview by reading the SQL and running against a copied database. | exit behavior is owned by the SQL client, with 0 on success and non-zero on SQL errors. |
| `scripts/quality/baseline.ts` | quality-pipeline | Generates Phase 0 historical baseline reports from SQLite image review data. | `--db`, `--out`, `--exclusions`, and `--project-root`. | Phase 0 CSV, Markdown, and JSON reports under the output directory. | no direct dry-run, but output can be redirected to a scratch directory with `--out`. | exits 0 when baseline criteria pass, non-zero when verification fails or inputs are invalid. |
| `scripts/quality/evaluate.ts` | quality-pipeline | Evaluates Phase 1 prediction files against labeled Phase 0 images. | `--phase`, `--split`, `--labeled`, `--predictions`, `--out`, and `--project-root`. | Phase 1 evaluation JSON and supporting reports under the output directory. | no direct dry-run, but output can be redirected to a scratch directory with `--out`. | exits 0 when evaluation criteria pass, non-zero on unsupported phases, missing predictions, or failed criteria. |
| `scripts/quality/review.ts` | review-images | Produces Phase 1 reviewer predictions with OpenAI-compatible or Codex vision backends. | `--phase`, `--backend`, `--labeled`, `--out`, `--limit`, `--offset`, `--project-title`, `--image-field`, `--resume`, `--model`, and `--project-root`. | JSONL prediction file and JSON summary on stdout. | preview with `--limit`, `--offset`, and scratch `--out`; `--resume` skips already written rows. | exits 0 when predictions are written or skipped, non-zero on invalid args, missing keys, image reads, or backend errors. |
| `scripts/quality/verify.ts` | quality-pipeline | Verifies Phase 0 or Phase 1 quality summary JSON files against acceptance criteria. | `--phase`, `--summary`, and `--out`. | JSON verification result on stdout. | read-only verification is the normal preview behavior. | exits 0 when criteria pass, non-zero when criteria fail or summary input is invalid. |
| `scripts/training/dataset-freeze-worker.ts` | training | CLI worker for dataset freeze tasks in the Training worker queue. | `--help`, `--once`, `--poll`, `--worker-owner`, and shared worker API env. | Worker task callbacks to Training HTTP routes plus console logs. | `--help` previews CLI usage without processing tasks. | exits 0 for help or completed once mode, non-zero on invalid args, API failures, or task failures. |
| `scripts/training/image-worker-runtime.ts` | training | Importable runtime for image generation worker provider selection, prompt resolution, and task completion. | Parsed image worker options and provider environment. | Worker callback payloads and generated image metadata for the entrypoint. | no direct CLI, use `scripts/training/image-worker.ts --help` or mock options for preview. | exit behavior is delegated to the image worker entrypoint. |
| `scripts/training/image-worker.ts` | training | CLI entrypoint for Training image generation tasks. | `--help`, `--once`, `--poll`, `--provider`, `--worker-owner`, mock flags, and provider config. | Generated images or mock outputs registered through Training worker callbacks. | `--help`, provider check mode, and mock flags preview behavior without full production generation. | exits 0 for help, provider checks, or successful work, non-zero on invalid args, provider errors, or task callback failures. |
| `scripts/training/training-worker-runtime.ts` | training | Importable runtime for local training runner checks, dry-run completion, and artifact shaping. | Parsed training worker options and local runner environment. | Training completion or failure payloads for the entrypoint. | no direct CLI, use `scripts/training/training-worker.ts --help` or dry-run options for preview. | exit behavior is delegated to the training worker entrypoint. |
| `scripts/training/training-worker.ts` | training | CLI entrypoint for Training run execution tasks. | `--help`, `--once`, `--poll`, `--worker-owner`, `--dry-run`, and `--mock-complete`. | Training run callback payloads, local runner artifacts, or mock completion payloads. | `--help`, `--dry-run`, and `--mock-complete` provide deterministic preview or mock behavior. | exits 0 for help or successful work, non-zero on invalid args, runner config failures, or task callback failures. |
| `scripts/training/worker-common.ts` | training | Shared Training worker HTTP client, argument parsing helpers, and polling loop utilities. | Worker API base URL, worker owner, once or poll mode, and callback payloads from entrypoints. | Lease, heartbeat, complete, fail, and scheduler HTTP calls for entrypoint scripts. | no direct CLI, entrypoint help commands preview the shared options. | exit behavior is delegated to the entrypoint using the shared helpers. |
| `scripts/training/worker-queue-runtime.ts` | training | Importable queue supervisor runtime for spawning and restarting Training worker child processes. | Worker queue options, child process command, mock flags, and shutdown signals. | Child worker processes plus prefixed child logs. | no direct CLI, use `scripts/training/worker-queue.ts --help` or mock flags for preview. | exit behavior is delegated to the queue entrypoint and child process lifecycle. |
| `scripts/training/worker-queue.ts` | training | CLI supervisor for Training image, dataset freeze, and training worker processes. | `--help`, child count options, worker owner prefix, mock image flags, dry-run training flags, and mock-complete flags. | Supervised child worker processes and console logs. | `--help` previews options, and mock flags run deterministic local worker flows. | exits 0 for help or clean shutdown, non-zero on invalid mock flag combinations or supervisor startup errors. |
| `src/scripts/backfill-comfy-output-subfolder.ts` | comfy-runtime | Backfills `Run.comfyOutputSubfolder` from submitted ComfyUI SaveImage prompt snapshots. | Current database connection through `src/lib/db`. | Updated run rows plus console counts for found, backfilled, and skipped rows. | dry-run is not supported, so inspect DB state or run against a copy before production use. | exits 0 if the script completes, non-zero if the top-level promise rejects. |
| `src/scripts/seed.mts` | seed-scripts | Seeds development data and placeholder image files for local generation review flows. | `DATABASE_URL`, optional `DB_PROVIDER`, generated Prisma clients, and local filesystem access. | Seeded database rows and placeholder files under `data/images/**`. | dry-run is not supported, so use a disposable or local development database. | exits 0 if seeding completes, non-zero on Prisma, adapter, or filesystem errors. |

## Python Environments

Keep Python scripts separate from Node scripts. Node services may spawn Python CLIs through explicit command env vars, but Python dependencies should stay in the selected Python or virtualenv instead of being imported by Node or bundled with `tsx` scripts.

- `scripts/auto-censor-mosaic.py` runs with `python3` by default through `AUTO_CENSOR_PYTHON_CMD` in the Node wrapper. Its Python environment must install `ultralytics`, `opencv-python`, and `pillow`, and `AUTO_CENSOR_MODEL_PATH` must point to the YOLO `.pt` model. The CLI help path must stay lightweight so `--help` works without importing heavy model dependencies.
- `scripts/fix-position-presets.py` is a legacy one-off formatter for `position_presets.md`. It is standard library only and should continue to run with the repository machine's `python3` unless it is replaced by a maintained Node or TypeScript script.

## Maintenance Rules

- Keep destructive scripts explicit about whether dry-run exists. If dry-run is absent, this document must say so.
- Generated docs scripts should be followed by a diff review before staging generated artifacts.
- Quality scripts must keep output paths configurable so experiments can write to scratch directories before replacing checked-in benchmark fixtures.
- Quality scripts must keep CSV serialization in `src/server/quality/csv-utils.ts` and JSON stdout behind exported typed CLI result builders.
- Quality analysis files under `docs/plans/auto-review-analysis/**` must be listed in `docs/plans/auto-review-analysis/README.md` as either regenerated artifacts or checked-in benchmark fixtures with an owner and regeneration or verification command. This directory is a quality-script data exception under `docs/plans/`; it is not an active execution-plan source.
- SQL files are script inputs rather than Node or Python CLIs; their preview and exit behavior come from the SQL client used to run them.
