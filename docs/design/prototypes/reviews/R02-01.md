---
schemaVersion: 1
document:
  type: design
  status: current
  owner: product-design
  authority:
    subject: new-frontend-ui-review-r02-01
    kind: reference
  readWhen:
    - 制作或审核全局导航外壳时
    - 区分导航占位与项目上下文或登录流程时
  sources:
    - docs/design/prototypes/ui-design-roadmap.md
    - docs/design/prototypes/ui-design-shared-plan.md
    - docs/design/prototypes/design-foundations.md
    - docs/design/prototypes/reviews/F-theme.md
    - docs/design/prototypes/shell/navigation/index.html
    - docs/design/prototypes/src/navigation.jsx
    - docs/design/prototypes/src/navigation.css
  verifiedBy:
    - npm --prefix docs/design/prototypes run build
    - npm run docs:check
---

# R02-01 全局导航外壳审核记录

## 事前登记与当前状态

本项已在[共享任务计划](../ui-design-shared-plan.md#r02-导航与登录)事前登记，属于 `R02`、覆盖 `C05`。用户于2026-09-09核对 `R01-01` 已确认、`R01-02` 暂停后，要求“先继续设计计划的下一项”；本轮据此选定既有 `R02-01`，没有新拆子项。本文先记录制作范围，再开始界面制作。**首稿已制作，待用户审核**；用户继续指令不是对具体布局成稿、全部79项或后续任务的批准。

用户目的：首次进入应用时到达生产任务；在生产／训练各四个一级入口和全局工具间切换，始终辨认当前模块与位置。`R01-02` 继续暂停，不作为本项输入。

当前呈现以文末[统一轨道与导航细节](#最新修订统一轨道与导航细节)为准：用户再次指出前版交互与细节不足，本轮继续修改同一任务；历史轨道、菜单及面板的测试结论不能代替新版验证。模块切换统一为轨道，退出登录保持移除，其他任务不随本轮展开。

## 输入、确定事项与本轮探索

业务来源是[总路线的D4输入](../ui-design-roadmap.md#输入来源与更新边界)，已重新读取本地重构检出的信息架构决策 `IA-01`～`IA-06`、`IA-16`。2026-09-10用户明确调整入口顺序与手机导航位置，以下按最新指令记录当前规则；原始 `IA-06` 的顶部模块布局被本次指令覆盖，不修改来源文档或回写历史验证结果。

| 输入 | 已确定事项 | 本轮呈现与边界 |
| --- | --- | --- |
| IA-01 | 首次／无有效记录进入生产任务，根入口恢复有效位置 | 用导航占位实现首次入口与有效顶层位置恢复；无效片段回退生产任务，不制作项目资源失效流程 |
| IA-02/03＋2026-09-10反馈 | 生产／训练同权、始终可见；两模块四入口统一按任务、项目、预制、模板排列 | 桌面与手机顺序一致；设计活动项、模块切换与入口级最后位置记忆，不隐藏训练或任务 |
| IA-04/05＋2026-09-10反馈 | 桌面侧栏顶部模块切换、中部四入口、底部全局模型、监控与日志、设置与主题；内容预留空间 | 用户已要求移除注销，覆盖来源旧记录；设计展开／收起的宽度、标签与当前定位，具体呈现仍待审核 |
| IA-06＋2026-09-10反馈 | 手机底部单排四入口与更多；模块、全局工具及主题在更多中，顶部留给页面实际操作 | 最新用户指令覆盖原顶部模块布局；更多不含注销，不沿用桌面同尺寸侧栏挤占手机内容 |
| IA-16 | 当前项不只靠颜色，支持键盘、跳至主内容、减少动态效果及浏览器返回 | 当前入口同时具备文字／图标与状态；使用简单片段导航验证前后行为 |
| 已确认基础＋2026-09-10反馈 | 统一主题、字体、密度、模块色、纯白浅色底与系统主题跟随；侧栏增加明暗快捷切换 | 沿用共享提供器与公开组件接口；手动选择持久化，仍可显式恢复系统跟随，不设隐式过期 |

## 本轮包含与排除

| 范围 | 本轮做什么 | 不做什么 |
| --- | --- | --- |
| 一级导航 | 生产／训练四入口切换、各模块记住最后入口、桌面收放、手机底栏 | 不设计项目页签、面包屑或项目操作；这些归R02-02 |
| 全局工具 | 模型、监控与日志、设置入口，以及手机更多的打开／关闭与当前定位 | 不制作模型文件列表、设置表单、监控数据、日志列表或业务接口 |
| 路由验证 | 用原型片段区分顶层入口；浏览器前后与刷新可回同一占位 | 片段不是获批业务路由；查询、展开、页签、滚动和失效资源恢复归R02-02 |
| 鉴权边界 | 按最新用户要求移除退出登录入口及模拟逻辑 | 不读取令牌、不清除真实认证、不绘制登录过程；登录与失效鉴权归R02-03 |
| 内容区域 | 清楚标注的占位，只给页面名称和导航定位信息 | 不以虚构业务表格、指标或卡片冒充页面已设计，不加概览仪表盘 |
| 适配与状态 | 桌面、窄屏、手机导航；当前项、收放、菜单、焦点及减少动态效果 | 不要求纯导航占位假装具备业务加载、错误、空列表或批量状态 |

原型目标：`shell/navigation/index.html`，源码 `src/navigation.jsx`、`src/navigation.css`，预览地址为[全局导航外壳](http://127.0.0.1:5178/shell/navigation/)。这些是原型文件与预览地址，不是正式应用路由。

## 验收与实际验证

以下为首稿的历史验证，保留当时布局与测量值；2026-09-10修订以文末反馈规则为准，旧手机顶部布局的通过记录不能代表新版已通过。

| 应检查的结果 | 实际结果 |
| --- | --- |
| 首次进入生产任务，跨模块返回其最后入口，全局工具保持独立身份 | 首次状态回到生产任务；生产预制→训练模板→生产，回到生产预制；全局模型／设置有独立当前项，手机底栏不误选业务项 |
| 桌面展开／收起不遮挡内容；窄屏仍能定位当前入口 | 1200px起默认展开232px；768～1199px默认80px窄栏，图标链接保留名称与提示。触屏窄栏用88px，生产／训练两字不换行、按钮40px；已验证手工收放与内容留位 |
| 手机顶部两模块与底部四入口均可达，更多包含全局工具和注销 | 320／393／640px保留顶部两模块和底部四链接；模块按钮40px、更多图标44px。852×393粗指针横屏采用手机结构。模型、监控与日志、设置及注销均在更多中；注销只产生预览提示，片段不变 |
| 浏览器前后、刷新与无效片段回退正确；不声称已实现项目恢复 | 后退回训练模板，前进／刷新恢复生产预制；无效片段回到生产任务。本项只保存顶层入口，不存项目查询／滚动状态 |
| 键盘焦点、跳至内容、菜单退出及当前项语义正确 | 跳到主内容聚焦 `navigation-main` 且不改变片段；更多内反向制表键到退出入口，退出键和遮罩可关闭并回到更多按钮。首次使用的侧栏缺少焦点约束，已改为 `Dialog` 的右侧模态呈现；侧栏收放控制目标也已更正；减少动态效果下收放过渡为 `0.01ms`，检查后恢复媒体设置 |
| 深浅主题及两模块颜色、适用视口无横向溢出 | 已切换浅／深与生产／训练四组合，浅色画布纯白、深色画布中性灰；320、393、640、768、960、1199、1200、1440、2560px均无页面横向溢出。所测控制台无警告或错误 |
| 原型构建、设计治理测试与文档检查 | 原型构建、19项设计治理测试和完整文档检查通过；文档零错误、零警告 |

本轮证据目录为本机 `.impeccable/review/r02-01/`，不跟踪提交。响应式结论来自实际布局读数与交互；强制视口截图在当前宿主有缩放失真，不视为真机像素验收。没有验证真实业务数据、鉴权或项目级恢复，也不沿用 `R01` 的检查代替本项。

原生链接承载片段导航，模块切换、收放、预览设置沿用 `Button`／`SelectButton`／`Dialog`。导航外层样式不修改组件库内部结构；本轮侧栏宽度和导航定位仍是候选呈现，需用户审核。

## 后续修订：收起侧栏的模块图标

用户要求将窄栏中的生产／训练文字改为图标。收起侧栏使用 `PrimeIcons` 的图片图标与训练芯片图标，直接通过 `Button` 的 `icon` 属性呈现，保留完整名称的无障碍标签、悬停提示和按下状态；展开侧栏及手机顶部仍用文字。961px窄栏实测两按钮均为36×36px，模块切换正常；展开侧栏后恢复文字。此修订替代上方收起状态的文字按钮描述，旧尺寸记录保留为历史。仅调整本项局部呈现，不扩大任务范围。

用户随后要求轨道样式：收起侧栏时两个图标位于同一条竖向圆角轨道，内边距4px，选中项使用原生圆形按钮高亮。通过外层布局与 `Button rounded` 公开属性实现，不绘制额外焦点层或修改内部图标；展开侧栏和手机顶部保持文字切换。

## 2026-09-10 用户反馈与修订规则

本次继续修订既有 `R02-01`，不新增任务或业务页面。上方图标、轨道与首稿验证段落是版本历史，涉及手机顶部的描述由本节替代。

| 用户反馈 | 当前修订规则 | 本次需验证 |
| --- | --- | --- |
| 窄屏主内容顶部没有留白 | 主内容四周保持对应视口的页面间距，顶部不能贴住页面操作区边线 | 手机、758px窄屏与桌面主内容顶部留白可见，底部内容不被导航遮挡 |
| 任务放在最上面 | 生产与训练均按任务、项目、预制、模板排序，桌面侧栏与手机底部一致 | 两模块首项和键盘顺序一致，位置记忆不被重排破坏 |
| 侧栏增加深浅色切换 | 增加明暗主题快捷按钮，使用共享主题偏好；手动选择持久化，可显式恢复系统跟随 | 按钮名称说明动作，切换生效；系统恢复入口仍可达 |
| 退出登录是否来自设计文档 | 来源 `IA-05/06` 已包含桌面注销与手机更多中的注销；保留入口，仍仅给出模拟提示 | 不读取或清除真实认证，不触发真实退出；真实鉴权继续归 `R02-03` |
| 收放时元素高度抖动 | 收放过渡中品牌、模块区与导航行保持稳定高度，标签不因宽度变化换行挤压 | 展开和收起均检查中间状态；减少动态效果偏好有效 |
| 手机顶部要留给页面操作 | 移除顶部全局品牌、模块切换与更多；模块切换、更多与四个入口共同组成底部导航 | 手机和窄屏底部各入口可达，模块与当前项明确，页面操作区可供后续任务使用 |

本轮实测393、758、1024、1440px视口无页面横向溢出；393与758px主内容顶部内边距均为16px，393px底栏约125px且内容保留对应占位。桌面收起与展开后的模块区、主导航、底部工具区纵坐标一致，导航行高约44px。主题快捷切换刷新后保留选择；手机更多中保留系统／浅色／深色入口，Escape可关闭。原型构建通过。本轮未重新验证粗指针横屏、系统主题变化与动画逐帧表现；强制视口截图仍有宿主缩放失真，尺寸以浏览器元素布局读数为准。当前仍待用户审核。

## 历史修订：居中与单排底栏

本节覆盖上方历史版本：按用户要求移除退出登录，包括桌面、更多中的入口及其模拟逻辑，后续登录与设置任务不再设计该操作。旧来源中包含注销的描述仅保留为历史依据。

收起侧栏的隐藏文字与当前项标记退出横向布局，所有导航图标及主题／收放按钮沿同一中心线排列。模块轨道使用低对比中性底色和透明边框，与侧栏融为一体。手机底部改为任务、项目、预制、模板、更多五项单排；模块切换、全局工具和主题选择位于更多中，页面顶部仍留给业务操作。

本轮原型构建通过；393px底栏实测约65px，无横向溢出，更多中的训练切换可用且无退出登录入口。窄侧栏七个导航图标中心坐标一致。强制视口截图有宿主缩放失真，尺寸以浏览器元素布局读数为准。当前待用户审核。

## 历史修订：模块菜单与底部面板

按用户对模块区域与更多抽屉的重新设计要求，本节替代上方轨道和右侧全高抽屉方案。侧栏改为44px高的当前模块入口：展开状态显示名称和下拉图标，收起状态显示当前模块图标；使用组件库原生菜单选择生产／训练，保留当前项勾选和入口记忆。

手机更多改为底部“导航与外观”面板，最大宽480px、高度随内容；顶部为当前模块分段选择，中间为全局工具，底部为主题行。使用原生对话框及分段选择，不修改组件内部结构。关闭、遮罩及键盘退出沿用组件行为。

原型构建通过；393×852与320×568下无横向溢出，面板约432px高且主题控件完整可见。已验证侧栏菜单切换模块及面板键盘退出。当前待用户审核。

## 最新修订：统一轨道与导航细节

2026-09-10用户对上一版给出八项反馈，明确要求模块统一采用轨道，不再在轨道与选择器之间切换。本节覆盖上方模块菜单、默认对话框标题区和底栏整项选中边框等历史呈现；仍只修订 `R02-01`，不新增设计任务、业务入口或鉴权行为。

| 编号 | 用户反馈及已有证据 | 本轮修订规则 | 新版验收重点 |
| --- | --- | --- | --- |
| 1 | 更多面板分隔线贴住首个控件 | 内容起始保留16px内边距，当前模块、工具、主题之间保留明确组间距 | 首行与容器边缘、相邻组和分隔线均有留白，不依赖控件内部补丁 |
| 2 | 模块和主题交互不统一 | 模块选择与更多／预览设置中的系统／浅色／深色复用同一轨道结构；侧栏明暗快捷按钮保留 | 同类选择使用相同选中、焦点与按下语义，手动主题及恢复系统跟随仍可用 |
| 3 | “导航与外观”默认标题区域浪费空间 | `Dialog` 通过公开 `showHeader=false` 关闭默认标题区，自有紧凑顶部行提供关闭按钮与可访问名称 | 顶部紧凑且关闭入口完整可达；模态名称、键盘约束、Escape和返回焦点有效 |
| 4 | 全部模块切换改轨道，不要时而轨道时而选择器 | 模块切换统一为外层轨道和原生 `Button`，移除模块菜单；桌面展开横向文字、收起纵向图标，手机更多保持同一交互 | 各视口能直接选择两模块，当前项不只靠颜色；图标保留完整名称，入口记忆不变 |
| 5 | 五项手机底栏拥挤 | 保留任务、项目、预制、模板、更多单排五项；移除整项大块选中外框，选中底色限定在图标区域，增加内容留白 | 320／393px下图标与文字不挤压，命中区完整，底部内容不被遮挡 |
| 6 | 训练模块手机底部仍有蓝绿色边框 | 导航使用局部纯灰背景与边框变量，活动态不再复用偏蓝灰的 `border-control`；保留已批准的训练粉与生产绿 | 浅／深主题下分别切换两模块，导航边框保持中性、活动色对应模块 |
| 7 | 桌面侧栏也存在上述偏色 | 同一导航局部中性色规则覆盖侧栏、轨道、分隔线及底栏，不重选全局基础色板 | 桌面展开／收起及手机采用同一中性色规则，没有训练态遗留绿蓝边框 |
| 8 | 收起侧栏的模块按钮无法点击 | 原按钮实测宽度仅15.2178px，源于 `width:auto` 覆盖纯图标按钮默认宽度；移除该覆盖根源，由新轨道保留完整按钮尺寸 | 不只测图标中心：点击按钮边缘也生效；完整命中区与视觉区域一致，键盘可操作 |

实现边界：沿用组件公开属性、原生按钮与链接，在自有轨道和布局容器表达样式，不修改组件内部图标／标签结构，不添加透明点击覆盖层。保留已确认的字号、触控普通控件40px／图标控件44px、纯白浅色页面底和系统主题跟随规则。

本轮实测：320×568、393×852、640×360、758×807、1024×768、1440×900均无页面横向溢出。393px更多面板约342px高，内容四周16px；两组轨道按钮高度44px，模块行与分隔线间隔16px。短横屏面板受可用高度约束，内容可滚动。底栏约81px高，五项保持单排，选中底色仅落在图标区域。

收起侧栏两模块按钮均约44×44px，四个方向距边缘4px的命中检查全部落在按钮内，实际生产／训练切换正常；全部侧栏图标中心约39.7px。展开／收起后主导航纵坐标一致。浅色侧栏为纯灰245／245／245，分隔线224／224／224；深色轨道52／52／52、分隔线66／66／66。主题切换、恢复系统选中、更多内反向制表焦点约束及退出键关闭后返回更多按钮均通过。宿主强制视口截图存在缩放失真，尺寸以浏览器布局读数为准；未声明真机触控和逐帧动画验收。

原型构建与19项设计文档治理测试通过；完整文档检查随本轮交付复核。当前没有本轮设计批准记录。

## 最新修订：模块入口进入任务

用户指出生产仍进入项目。原因为模块切换恢复了此前保存的项目入口，覆盖了首次访问的任务默认值。现规定点击生产／训练轨道均进入对应任务页，移除各模块上次入口记忆；本节覆盖上方跨模块记忆的历史行为。显式页面地址、刷新当前页、浏览器前后及应用启动恢复有效上次页面的规则继续保留。

## 2026-09-11 修订：同步侧栏收放

用户指出内部元素与侧栏伸缩不同步、收起时生产文字溢出。原先宽度渐变叠加即时居中切换和标签插入／移除，形成两个不同的布局节奏。现使用单一收放进度驱动侧栏宽度与图标偏移；按钮保留稳定结构，通过组件公开插槽传入同步样式。展开文字按可用空间渐显，收起时立即隐藏；侧栏允许缩小并明确裁切边界。模块、按钮与导航行高度不变。

浏览器按帧采样了展开、收起与快速反向切换：侧栏右边界与内容区左边界最大差值为0，导航图标纵坐标变化为0，反向切换从当前进度返回，未跳到端点。收起的两模块文字计算可见性均为隐藏，按钮焦点保留；减少动态效果下过渡为0.01毫秒。393px手机布局无横向溢出。采样仅临时读取布局，结束已移除采样数据并恢复媒体和视口设置；不以历史静态截图替代本次动画验证。

## 2026-09-11 修订：主题入口与侧栏项一致

主题快捷操作复用侧栏项的44px行高、16px图标、12px间距及文字样式，使用原生按钮保留操作语义。文字与其他侧栏项共用显隐规则，不再叠加组件按钮标签自身的过渡；主题切换及系统偏好规则保持不变。

## 2026-09-11 修订：全宽更多与双选项主题

用户要求窄屏更多占满宽度、去掉“模块”标签、切换模块后保持面板打开，并更新主题选择规则。面板现与视口等宽，顶行左侧是生产／训练轨道、右侧关闭；中间三个全局工具并排，底部浅色／深色轨道。只有进入具体工具、关闭、遮罩和退出键收起面板；模块切换保留面板并进入对应任务页。

主题不再提供系统选项。默认实时跟随系统；用户选与系统当前颜色不同的外观则固定，选回与系统相同的外观则恢复跟随。系统变化本身不会解除固定选择。共享偏好和基础、列表、组织原型的主题控件同步此规则，选择态反映实际显示的浅／深色；本节覆盖历史三选项及显式恢复入口规则。

本轮607、393、320px实测面板与视口等宽，面板约282px高，无页面横向溢出，320px三个工具标签完整。已验证跨模块保持打开；系统颜色变化时自动跟随、选择相反色后固定且刷新保留、系统往返变化不解除固定、选择回系统色后再次跟随。原型构建通过。

## 2026-09-11 修订：紧凑更多与一致触摸反馈

按最新反馈，手机布局的底栏与更多统一不显示悬停底色，保留按下、选中与键盘焦点；鼠标桌面侧栏继续有悬停反馈。取消更多中的“主题”文字与独立主题行，太阳／月亮图标轨道与生产／训练同排，保留浅色／深色的可访问名称。工具行在常规窄屏为44px高、图标与文字同排；不足400px时采用52px高的上下排列，保证长标签完整与触控尺寸。

607px实测面板约154px高，320px约162px高且无溢出；两个轨道与44px关闭按钮均完整。鼠标实际悬停更多工具和底部入口时，背景均保持透明。主题图标仍调用共享跟随／固定规则，本轮不改变偏好逻辑。

## 用户审核与下一步

当前没有本项成稿批准记录。审核只关闭本文的全局导航外壳范围；`R02-02`、`R02-03` 与其他任务仍按各自条目确认，不因本轮完成自动制作。

## 上级路由

- [设计文档](../../README.md)
- [总路线](../ui-design-roadmap.md)
- [共享任务计划](../ui-design-shared-plan.md)
