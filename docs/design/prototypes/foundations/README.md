---
schemaVersion: 1
document:
  type: router
  status: current
  owner: product-design
  authority:
    subject: prototype-design-foundations
    kind: router
  readWhen:
    - 审核新版基础色彩、字体、尺寸与组件状态时
    - 按编号复用或调整已确认基础设计时
  sources:
    - docs/design/prototypes/foundations/index.html
    - docs/design/prototypes/src/main.jsx
    - docs/design/prototypes/src/tokens.css
    - docs/design/prototypes/src/prototype.css
    - docs/design/prototypes/src/foundations.css
    - docs/design/prototypes/reviews/F-responsive.md
  verifiedBy:
    - npm --prefix docs/design/prototypes run build
    - npm run docs:check
---

# 基础设计确认清单

用户于 **2026-09-08** 确认了 `F-01`～`F-14` 基础设计，批准基线 **`8029fdfb`** 包括各视口适配、640px 同排组合上限与浅色纯白页面底色；其余浅色中性色去色比较未采纳。该批准历史保留。本轮按用户要求只以 `InputNumber` 撤去非必要内部覆盖，恢复原生横向步进与焦点，指定实测已记录；其他已确认基础通用控件保持既有样式。`R01-01` 仍暂停，相关改动本轮未验证，不扩大基础重审或批准后续页面。

完整规则见[新版前端基础设计规范](../design-foundations.md)，可操作样本见[基础设计原型](http://127.0.0.1:5178/foundations/)，启动命令见[原型说明](../README.md)。本清单区分已确认基线与数字输入本轮原生修订；指定实测、真实限制及时间线见[审核记录](../reviews/F-responsive.md)，不包括业务页面或生产实施确认。

## 视觉基础

| 编号 | 元素 | 已确认规则 | 后续复核重点 | 状态 |
| --- | --- | --- | --- | --- |
| F-01 | 主题与表面 | 明、暗两套主题；浅色页面底色纯白，其他浅灰表面与边界按现行基线；深色黑白灰无蓝色底调；透明层级用于确有层次关系的区域 | 连续阅读、边界与容器层级；不采用其余浅色去色比较 | 已确认 |
| F-02 | 模块强调色 | 按用户后续配置，图像生产采用 `#176E5A` 色相，`LoRA` 训练采用 `#A23F5D` 色相，各主题保持现有饱和度、明度与层级；柔和强调背景文字使用 `on-accent-soft` | 两模块同等醒目，深浅主题清晰 | 已确认，色相按本轮指示更新 |
| F-03 | 状态语义 | 成功、失败、警告与信息独立于模块色；暂停样本沿用警告语义；同时呈现文字或图标 | 不把模块色误读为结果状态，不仅凭颜色理解 | 已确认 |
| F-04 | 字体组合 | 英文与数字使用 `Geist`，中文使用 `Noto Sans SC`；参数、标识符和代码使用 `IBM Plex Mono` | 中英文混排、小字号与长参数可读性 | 已确认 |
| F-05 | 字号与行高 | `12 / 14 / 16 / 20 / 24 / 32px`；正文 `14px`、行高 `22px`；`12px` 用于辅助信息 | 正文密度、标题占用与辅助文字可读性 | 已确认 |
| F-06 | 间距 | `4px` 为基准，展示 `4 / 8 / 12 / 16 / 24 / 32 / 48px` 等常用层级 | 标签与控件、组内与组间关系 | 已确认 |
| F-07 | 尺寸与圆角 | 通用标准密度与 `640px` 短输入组合上限保留；数字输入本轮采用原生尺寸，手机根与两按钮实际高 `40px`、无额外透明热区 | 通用目标与各组件实际命中分开记录，不将数字输入描述为有 `44px` 隐形目标 | 批准基线保留；数字修订的指定尺寸已实测 |

实际色值以页面色样及 `src/tokens.css` 为同一份来源，不在说明中维护第二份十六进制清单。`F-02` 的两个颜色是用户指定的色相参考，不要求明暗主题按钮都直接使用该原始色值。语义变量先表达用途，再由主题和模块上下文提供具体颜色。

## 基础组件

| 编号 | 元素 | 已确认规则与样本 | 后续复核重点 | 状态 |
| --- | --- | --- | --- | --- |
| F-08 | 按钮与图标 | 主次操作、危险／加载／禁用语义及 `PrimeIcons`；纯图标居中且不保留文字间隙 | 各设备尺寸、同排对齐与实际热区 | 已确认 |
| F-09 | 文字与数值输入 | 文字输入的已确认语义与样式保留；`InputNumber` 恢复公开属性配置的原生横向步进及 `Lara` 焦点形状，只映射模块颜色 | 已测 4→5→键盘返回4及 `Tab` 离开、手机40px无伪元素；不强造整圈焦点 | `8029fdfb` 历史保留；本轮原生修订及指定实测已记录 |
| F-10 | 选择控件 | 单多选下拉、复选框、单选框和开关；下拉只扩外沿，多选以名称／数量摘要展示并在菜单内取消 | 外壳、44px 菜单目标、键盘状态与真实边缘点击 | 已确认 |
| F-11 | 标签与轻量反馈 | 状态文案、图标和颜色语义，加载／错误／空内容样本按设备重排 | 状态可见性与恢复；验证证据的实际覆盖范围仍需区分 | 已确认 |
| F-12 | 小型结构 | 分段／页签、宽屏目录、窄屏章节下拉与预览弹窗 | 章节选择、背景滚动锁定、关闭后焦点返回 | 已确认 |

样本继续使用 `PrimeReact v10`。用户明确要求“如非必要，不要对组件库做侵入式改动”：优先公开属性、主题配置与外层布局，不为像素对齐修改内部 `DOM`、模拟焦点或添加透明点击层。确有产品或无障碍必要时，才采用最小且可说明的覆盖并记录验证。本轮落实对象仅为 `InputNumber`；已批准颜色、字体和通用标准密度的必要主题映射保留。

## 交互与适配

| 编号 | 元素 | 已确认规则 | 后续复核重点 | 状态 |
| --- | --- | --- | --- | --- |
| F-13 | 主题操作 | 默认实时跟随系统；手动选择明或暗后持久保存；提供明确“改为跟随系统”入口 | 刷新保留手动选择，重置立即回到系统主题 | 已确认 |
| F-14 | 键盘、移动与动效 | 可见焦点、具名按钮、触摸目标、减少动态效果、连续视口规则与短横屏布局 | 八视口矩阵、主流程、审查结论与验证限制分别记录，不只判断无溢出 | 已确认 |

主题、模块关系及批准基线设备呈现的确认继续保留。数字输入本轮修订不沿用旧实测宣称通过；禁用状态不代替错误说明，颜色不成为唯一状态信号，浮层继承主题与模块。后续变更仍按实际影响范围记录。

## 后续复用与调整

1. `InputNumber` 本轮原生修订和指定实测已记录，R01 既有稿及改动仍保留，列表工作暂停且本轮未验证。
2. 后续按当前任务指示继续；本次不批准 `R01` 全批次，也不自动开始后续页面。改变基础时说明必要性，并同步样本、规范与对应 `F` 编号记录。
3. 本轮及以后每轮均同时完成两模块、明暗主题和适用状态的桌面、中间宽度及手机方案；分别验证视觉尺寸、布局、首屏优先级、真实热区、键盘和浮层。

后续反馈仍可直接写“F-05 正文改为 15px”或“F-07 保留 36px”。本次确认不取消后续调整，也不把尚未设计或尚未审核的业务组合与页面记为已通过；页面目录、示例文案和模拟业务数据不因此成为业务需求。

## 上级路由

- [HTML 设计原型](../README.md)
- [设计文档](../../README.md)
