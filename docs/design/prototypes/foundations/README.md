---
schemaVersion: 1
document:
  type: router
  status: current
  owner: product-design
  authority:
    subject: prototype-design-foundations
    kind: router
  readWhen:
    - 审核新版基础色彩、字体、尺寸与组件状态时
    - 按编号复用或调整已确认基础设计时
  sources:
    - docs/design/prototypes/foundations/index.html
    - docs/design/prototypes/src/main.jsx
    - docs/design/prototypes/src/tokens.css
    - docs/design/prototypes/src/prototype-provider.jsx
    - docs/design/prototypes/src/prototype-layout.css
    - docs/design/prototypes/src/theme/primereact.css
    - docs/design/prototypes/scripts/build-theme.mjs
    - docs/design/prototypes/src/foundations.css
    - docs/design/prototypes/reviews/F-responsive.md
    - docs/design/prototypes/reviews/F-theme.md
  verifiedBy:
    - npm --prefix docs/design/prototypes run build
    - npm run docs:check
---

# 基础设计确认清单

`8029fdfb` 的基础批准历史保留。**基础统一主题版已制作，待用户审核**：色相、浅色白底、字体和页面布局保持，基础入口使用固定官方源生成的完整组件主题。指定四主题、尺寸与交互检查已记录，但旧批准不能代替新主题的用户确认。实际范围及后续交付结果见[统一主题审核页](../reviews/F-theme.md)；暂停的列表稿尚未迁移。

完整规则见[基础规范](../design-foundations.md)，样本见[基础原型](http://127.0.0.1:5178/foundations/)，启动方式见[原型说明](../README.md)。本清单区分保留的设计方向与本轮待审核的生成主题；旧适配和原生数字修订保存在[历史记录](../reviews/F-responsive.md)，不作为本轮验证。

## 视觉基础

| 编号 | 元素 | 已确认规则 | 后续复核重点 | 状态 |
| --- | --- | --- | --- | --- |
| F-01 | 主题与表面 | 明、暗两套主题；浅色页面底色纯白，其他浅灰表面与边界按现行基线；深色黑白灰无蓝色底调；透明层级用于确有层次关系的区域 | 连续阅读、边界与容器层级；不采用其余浅色去色比较 | 已确认 |
| F-02 | 模块强调色 | 按用户后续配置，图像生产采用 `#176E5A` 色相，`LoRA` 训练采用 `#A23F5D` 色相，各主题保持现有饱和度、明度与层级；柔和强调背景文字使用 `on-accent-soft` | 两模块同等醒目，深浅主题清晰 | 已确认，色相按本轮指示更新 |
| F-03 | 状态语义 | 成功、失败、警告与信息独立于模块色；暂停样本沿用警告语义；同时呈现文字或图标 | 不把模块色误读为结果状态，不仅凭颜色理解 | 已确认 |
| F-04 | 字体组合 | 英文与数字使用 `Geist`，中文使用 `Noto Sans SC`；参数、标识符和代码使用 `IBM Plex Mono` | 中英文混排、小字号与长参数可读性 | 已确认 |
| F-05 | 字号与行高 | `12 / 14 / 16 / 20 / 24 / 32px`；正文 `14px`、行高 `22px`；`12px` 用于辅助信息 | 正文密度、标题占用与辅助文字可读性 | 已确认 |
| F-06 | 间距 | `4px` 为基准，展示 `4 / 8 / 12 / 16 / 24 / 32 / 48px` 等常用层级 | 标签与控件、组内与组间关系 | 已确认 |
| F-07 | 尺寸与圆角 | 官方主题参数生成标准与触控尺寸；本轮六视口已测约36／44px表单高度及281～640px短组合宽度 | 真实尺寸与原生小选择控件分开验证，不采用透明层扩大命中 | 新主题已制作，待用户审核；所测尺寸见记录 |

实际色值以页面色样及 `src/tokens.css` 为同一份来源，不在说明中维护第二份十六进制清单。`F-02` 的两个颜色是用户指定的色相参考，不要求明暗主题按钮都直接使用该原始色值。语义变量先表达用途，再由主题和模块上下文提供具体颜色。

## 基础组件

| 编号 | 元素 | 已确认规则与样本 | 后续复核重点 | 状态 |
| --- | --- | --- | --- | --- |
| F-08 | 按钮与图标 | 操作语义与图标系统保留，内部结构和状态来自生成主题；四主题主按钮颜色已检查 | 实际检查范围单列，不把主按钮样本扩大为全部按钮状态 | 新主题已制作，待用户审核 |
| F-09 | 文字与数值输入 | 文本／数值使用统一主题，空标签与透明层残留为0；步进、键盘移出、空名称错误与保存已测 | 指定操作及输入36／44px尺寸见记录；旧数值检查仅属历史 | 新主题已制作，待用户审核 |
| F-10 | 选择控件 | 原生小选择控件与标签配合；已测复选／开关标签、单选“精细”、多选“名称＋状态”及四主题下拉选中／聚焦可读性 | 记录所测范围及真实限制，不采用透明点击层 | 新主题已制作，待用户审核 |
| F-11 | 标签与轻量反馈 | `Tag` 通过统一提供器公开根插槽保留柔和语义色；加载4个骨架、失败／重试和空内容恢复已测 | 公开根颜色映射为最小例外，不改内部结构 | 新主题已制作，待用户审核 |
| F-12 | 小型结构 | 库配置集中到提供器；已测章节菜单滚动、确认／设置弹窗背景锁定和焦点返回，短横屏设置可见 | 公开属性与根槽位配置，所测路径和截图限制另列 | 新主题已制作，待用户审核 |

样本继续使用 `PrimeReact 10.9.9`，不升级组件库。按非必要不侵入原则，基础新版通过统一官方主题与公开属性表达组件能力，移除内部补丁、透明点击层与隐藏输入偏移；外层文件只负责页面布局。R01仍使用旧 `prototype.css`，待后续迁移再删除，不宣称本轮整个原型工程已统一。

## 交互与适配

| 编号 | 元素 | 已确认规则 | 后续复核重点 | 状态 |
| --- | --- | --- | --- | --- |
| F-13 | 主题操作 | 默认实时跟随系统；手动选择明或暗后持久保存；提供明确“改为跟随系统”入口 | 刷新保留手动选择，重置立即回到系统主题 | 已确认 |
| F-14 | 键盘、移动与动效 | 六视口矩阵、1440px粗指针44px输入、短横屏和减少动态效果骨架已检查 | 强制视口以实际测量和交互为主，不以异常截图证明完整视觉覆盖；真机／读屏未验 | 新主题已制作，待用户审核 |

主题、模块关系和页面布局方向保留；本轮生成主题的呈现与状态仍待实际验证。禁用状态不代替错误说明，颜色不成为唯一状态信号；统一入口必须让浮层正确使用主题与模块上下文。

## 后续复用与调整

1. 当前先制作并审核[基础统一主题版](../reviews/F-theme.md)，R01既有稿及改动保留，列表工作仍暂停且未迁移。
2. 主题版取得实际验证及用户意见后按下一步范围处理；本次不批准R01全批次，也不自动开始业务页面。后续迁移完成再删除旧样式文件。
3. 本轮及以后每轮均同时完成两模块、明暗主题和适用状态的桌面、中间宽度及手机方案；分别验证视觉尺寸、布局、首屏优先级、真实热区、键盘和浮层。

后续反馈仍可直接写“F-05 正文改为 15px”或“F-07 保留 36px”。本次确认不取消后续调整，也不把尚未设计或尚未审核的业务组合与页面记为已通过；页面目录、示例文案和模拟业务数据不因此成为业务需求。

## 上级路由

- [HTML 设计原型](../README.md)
- [设计文档](../../README.md)
