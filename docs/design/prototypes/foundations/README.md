---
schemaVersion: 1
document:
  type: router
  status: current
  owner: product-design
  authority:
    subject: prototype-design-foundations
    kind: router
  readWhen:
    - 审核新版基础色彩、字体、尺寸与组件状态时
    - 按编号复用或调整已确认基础设计时
  sources:
    - docs/design/prototypes/foundations/index.html
    - docs/design/prototypes/src/main.jsx
    - docs/design/prototypes/src/tokens.css
    - docs/design/prototypes/src/prototype.css
    - docs/design/prototypes/src/foundations.css
    - docs/design/prototypes/reviews/F-responsive.md
  verifiedBy:
    - npm --prefix docs/design/prototypes run build
    - npm run docs:check
---

# 基础设计确认清单

用户于 2026-09-08 确认了原始基础设计，视觉基线源提交为 `7fdf13de`。当前基础页各视口修订稿已具备：`F-07` 的设备尺寸、`F-08`～`F-12` 的设备呈现和 `F-14` 响应式适配**待用户复审**，见[本轮审计与审核记录](../reviews/F-responsive.md)。已明确的色相、字体、语义规则和固定标准密度继续保留，不取消全部基础确认。后续列表与页面设计仍暂停，待本轮基础适配获用户确认后恢复。

完整规则见[新版前端基础设计规范](../design-foundations.md)，可操作样本见[基础设计原型](http://127.0.0.1:5178/foundations/)，启动命令见[原型说明](../README.md)。本清单区分保留规则与重新审核的设备呈现；不包括业务页面或生产实施确认。

## 视觉基础

| 编号 | 元素 | 已确认规则 | 后续复核重点 | 状态 |
| --- | --- | --- | --- | --- |
| F-01 | 主题与表面 | 明、暗两套主题；柔和中性色背景、可区分的内容表面与浮层；深色黑白灰无蓝色底调；透明层级用于确有层次关系的区域 | 连续阅读、边界与容器层级 | 已确认 |
| F-02 | 模块强调色 | 按用户后续配置，图像生产采用 `#176E5A` 色相，`LoRA` 训练采用 `#A23F5D` 色相，各主题保持现有饱和度、明度与层级；柔和强调背景文字使用 `on-accent-soft` | 两模块同等醒目，深浅主题清晰 | 已确认，色相按本轮指示更新 |
| F-03 | 状态语义 | 成功、失败、警告与信息独立于模块色；暂停样本沿用警告语义；同时呈现文字或图标 | 不把模块色误读为结果状态，不仅凭颜色理解 | 已确认 |
| F-04 | 字体组合 | 英文与数字使用 `Geist`，中文使用 `Noto Sans SC`；参数、标识符和代码使用 `IBM Plex Mono` | 中英文混排、小字号与长参数可读性 | 已确认 |
| F-05 | 字号与行高 | `12 / 14 / 16 / 20 / 24 / 32px`；正文 `14px`、行高 `22px`；`12px` 用于辅助信息 | 正文密度、标题占用与辅助文字可读性 | 已确认 |
| F-06 | 间距 | `4px` 为基准，展示 `4 / 8 / 12 / 16 / 24 / 32 / 48px` 等常用层级 | 标签与控件、组内与组间关系 | 已确认 |
| F-07 | 尺寸与圆角 | 固定标准密度保持：精细指针桌面通用控件高 `36px`；候选手机／粗指针为 `40px` 外观、至少 `44px` 触摸目标；圆角角色保留，无密度选择或保存偏好 | 同排输入／按钮等高、真实边缘点击及大小屏的粗指针目标 | 标准密度与圆角原则保留；设备修订稿待复审 |

实际色值以页面色样及 `src/tokens.css` 为同一份来源，不在说明中维护第二份十六进制清单。`F-02` 的两个颜色是用户指定的色相参考，不要求明暗主题按钮都直接使用该原始色值。语义变量先表达用途，再由主题和模块上下文提供具体颜色。

## 基础组件

| 编号 | 元素 | 已确认规则与样本 | 后续复核重点 | 状态 |
| --- | --- | --- | --- | --- |
| F-08 | 按钮与图标 | 主次操作、危险／加载／禁用语义及 `PrimeIcons` 保留；纯图标居中且不保留文字间隙 | 已形成设备尺寸与同排对齐样本，按实际热区复核 | 组件原则保留；设备修订稿待复审 |
| F-09 | 文字与数值输入 | 常驻标签、说明、校验、只读／禁用及等宽参数保留；空名称提交可聚焦错误字段 | 各设备输入／步进器等高、键盘步进、长内容与错误警报 | 组件原则保留；设备修订稿待复审 |
| F-10 | 选择控件 | 单多选下拉、复选框、单选框和开关语义保留；下拉只扩外沿，多选以名称／数量摘要展示并在菜单内取消 | 外壳、44px 菜单目标、键盘状态与真实边缘点击 | 组件原则保留；设备修订稿待复审 |
| F-11 | 标签与轻量反馈 | 状态文案、图标和颜色语义保留，加载／错误／空内容样本按设备重排 | 已展示状态的可见性与恢复，不将历史验证充当本轮结果 | 组件原则保留；设备修订稿待复审 |
| F-12 | 小型结构 | 分段／页签用途保留；宽屏目录、窄屏章节下拉与预览弹窗形成候选 | 章节选择、背景滚动锁定、关闭后焦点返回 | 组件原则保留；设备修订稿待复审 |

样本继续使用 `PrimeReact v10` 现成控件。本轮重新审核基础页各视口的外观及通用交互，不改变任务提交、图片审核、训练状态机等业务边界。

## 交互与适配

| 编号 | 元素 | 已确认规则 | 后续复核重点 | 状态 |
| --- | --- | --- | --- | --- |
| F-13 | 主题操作 | 默认实时跟随系统；手动选择明或暗后持久保存；提供明确“改为跟随系统”入口 | 刷新保留手动选择，重置立即回到系统主题 | 已确认 |
| F-14 | 键盘、移动与动效 | 可见焦点、具名按钮、触摸目标和减少动态效果原则保留；连续视口规则与短横屏布局形成候选 | 八视口矩阵、主流程、审查结论与验证限制分别记录，不只判断无溢出 | 原则保留；适配修订稿待复审 |

主题行为与模块关系继续有效；标为重新审核的设备呈现需本轮实际样本与用户确认，不以旧确认直接通过。禁用状态不应代替错误说明，颜色不应成为唯一状态信号；浮层须继承当前主题与模块上下文。

## 后续复用与调整

1. 当前暂停新业务组合及页面设计，先完成[基础各视口适配](../reviews/F-responsive.md)；已有列表稿保留。
2. 对 `F-07`、`F-08`～`F-12` 的设备呈现和 `F-14` 补齐实际样本、同轮证据及用户确认，再同步规范与变量，恢复后续路线。
3. 本轮及以后每轮均同时完成两模块、明暗主题和适用状态的桌面、中间宽度及手机方案；分别验证视觉尺寸、布局、首屏优先级、真实热区、键盘和浮层。

后续反馈仍可直接写“F-05 正文改为 15px”或“F-07 保留 36px”。本次确认不取消后续调整，也不把尚未设计或尚未审核的业务组合与页面记为已通过；页面目录、示例文案和模拟业务数据不因此成为业务需求。

## 上级路由

- [HTML 设计原型](../README.md)
- [设计文档](../../README.md)
