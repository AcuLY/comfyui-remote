# Prisma Schema Compatibility Checklist

Generated by `scripts/docs/generate-prisma-schema-compatibility.ts`. Re-run after adding, removing, or renaming Prisma models.

## Shared Models

| model | owner domain | PostgreSQL schema | SQLite schema | compatibility status | action |
| --- | --- | --- | --- | --- | --- |
| `AuditLog` | operations | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `CensoringTask` | review-images | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `GpuTaskLock` | operations | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `ImageResult` | review-images | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `LoraAsset` | asset-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `Preset` | preset-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `PresetCategory` | preset-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `PresetCategorySlot` | preset-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `PresetChangeLog` | preset-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `PresetFolder` | preset-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `PresetGroup` | preset-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `PresetGroupChangeLog` | preset-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `PresetGroupMember` | preset-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `PresetVariant` | preset-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `PresetVariantLink` | preset-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `Project` | generation-projects | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `ProjectFolder` | generation-projects | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `ProjectPresetBinding` | generation-projects | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `ProjectSection` | generation-projects | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `ProjectSectionFolder` | generation-projects | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `ProjectTemplate` | template-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `ProjectTemplatePresetBinding` | template-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `ProjectTemplateSection` | template-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `ProjectTemplateSectionFolder` | template-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `Run` | generation-projects | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `SectionChangeLog` | generation-projects | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `SectionManualLoraEntry` | generation-projects | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `SectionPresetBinding` | generation-projects | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `SectionPromptBlock` | generation-projects | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TemplateSectionManualLoraEntry` | template-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TemplateSectionPresetBinding` | template-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TemplateSectionPromptBlock` | template-library | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingArtifact` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingCharacterImage` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingCharacterProfile` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingDatasetRevision` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingDatasetRevisionItem` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingGenerationInputReference` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingGenerationTask` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingGenerationTaskOutput` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingImageResult` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingProject` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingRun` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingSceneDescriptionBlock` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingSceneDescriptionPreset` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingSceneDescriptionPresetCategory` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingSceneDescriptionPresetFolder` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingSection` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingSectionRun` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingTemplate` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingTemplateSection` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingTemplateSectionSceneDescriptionBlock` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrainingTextRevision` | training | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |
| `TrashRecord` | review-images | `prisma/schema.prisma` | `prisma/schema.sqlite.prisma` | shared in PostgreSQL and SQLite | keep-compatible; update both schemas or document provider-specific difference |

## Provider Enum Mapping

| enum | PostgreSQL values | PostgreSQL fields | SQLite fields | compatibility action |
| --- | --- | --- | --- | --- |
| `JobStatus` | `draft`, `queued`, `running`, `partial_done`, `done`, `failed` | `Project.status` | `Project.status: String @default("draft")` | PostgreSQL uses Prisma enum columns; SQLite stores equivalent strings and must keep values/defaults synchronized. |
| `RunStatus` | `queued`, `running`, `done`, `failed`, `cancelled`, `paused` | `Run.status` | `Run.status: String @default("queued")` | PostgreSQL uses Prisma enum columns; SQLite stores equivalent strings and must keep values/defaults synchronized. |
| `ReviewStatus` | `pending`, `kept`, `trashed` | `ImageResult.reviewStatus` | `ImageResult.reviewStatus: String @default("pending")` | PostgreSQL uses Prisma enum columns; SQLite stores equivalent strings and must keep values/defaults synchronized. |
| `ActorType` | `user`, `system`, `agent` | `TrashRecord.actorType`, `AuditLog.actorType` | `TrashRecord.actorType: String @default("user")`, `AuditLog.actorType: String @default("system")` | PostgreSQL uses Prisma enum columns; SQLite stores equivalent strings and must keep values/defaults synchronized. |
| `PromptBlockType` | `custom`, `preset` | `TemplateSectionPromptBlock.type`, `SectionPromptBlock.type` | `TemplateSectionPromptBlock.type: String @default("custom")`, `SectionPromptBlock.type: String @default("custom")` | PostgreSQL uses Prisma enum columns; SQLite stores equivalent strings and must keep values/defaults synchronized. |

## Provider-Only Models

- PostgreSQL-only models: none
- SQLite-only models: none

## Checklist Rule

- Shared model changes must update both `prisma/schema.prisma` and `prisma/schema.sqlite.prisma` in the same batch.
- Provider-specific differences must be documented in `docs/prisma-provider-matrix.md` or in the batch notes before merging.
- Regenerate both clients with `npm run prisma:generate:all` after schema changes.
